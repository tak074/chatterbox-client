//components inside the input box
//this will go through messageView.js
// this.userName: window.location.search.slice(10),
// this.text:
// this.roomName:
class Messages {
  constuctor (userName, test, roomName) {
    this.userName = username;
    this.text = tet;
    this.roomName = roomName;
  }
}